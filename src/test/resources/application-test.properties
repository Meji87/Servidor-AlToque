# BD de PRUEBAS (separada de la de desarrollo)
spring.datasource.url=jdbc:postgresql://localhost:5432/altoque_test
spring.datasource.username=altoque
spring.datasource.password=altoque

# Esquema para tests (elige una opción)
spring.jpa.hibernate.ddl-auto=create-drop
# Si usas Flyway en tests, cambia a:
# spring.jpa.hibernate.ddl-auto=validate
# spring.flyway.clean-disabled=false

